{
  "name": "locke-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Locke's backend: proxies photo-verification calls to Claude (so the API key never ships in the app), the accountability-buddy pairing/sync API, and Google Play subscription verification/entitlement.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "googleapis": "^144.0.0"
  }
}
