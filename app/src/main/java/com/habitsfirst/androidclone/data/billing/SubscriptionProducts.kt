package com.habitsfirst.androidclone.data.billing

/**
 * Placeholder Play Billing product/subscription IDs. These are not wired up to any
 * Play Billing API call yet -- once real billing lands, these must be updated to match
 * whatever subscriptions/in-app products are actually configured in Play Console.
 */
object SubscriptionProducts {
    const val MONTHLY_SUBSCRIPTION_ID = "locke_premium_monthly"
    const val ANNUAL_SUBSCRIPTION_ID = "locke_premium_annual"
    const val LIFETIME_PRODUCT_ID = "locke_premium_lifetime"
}
